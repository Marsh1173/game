export const config = {
    playerSize: 50, // Decides player height and width
    jumpSize: 20, // Decides player jump height
    playerCount: 3, // from 1-4, decides # of players
    blastCooldown: 20, // Cooldown of player blast ability
};
